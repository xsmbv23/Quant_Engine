# RESEARCH PIVOT HANDOFF

Quant Engine must not confuse parser/evidence hardening with quantitative research progress.

## Current room state

```text
ROOM 01 INPUT ADAPTER = HARDENED
ROOM 02 RESEARCH DATASET = LOCKED
```

Room 02 remains locked until canonical real data is admitted by the Brain-authoritative admission chain.

## Required transition

```text
CANONICAL REAL DATA
  -> DATE-ALIGNED RESEARCH DATASET
  -> NO-LOOKAHEAD VALIDATION
  -> REPRODUCIBLE BASELINE RESEARCH
  -> BOUNDED BACKTEST
  -> EDGE
  -> EV/P&L/ROI
```

## Non-negotiable rules

- Synthetic production data is forbidden.
- Fixtures are not reality.
- Event/result date and source identity must be explicit.
- Information timestamp must be explicit.
- No feature may use information unavailable at decision time.
- Raw byte hash and semantic canonical hash are distinct.
- Unknown values remain unknown.
- Ads, navigation, chrome, and generic page numbers are never truth.
- Official result panel is required.
- Heavy research is external/bounded/chunked to protect Render Free 512 MB.
- Kelly remains downstream-only after a validated edge.
- Multiple-testing correction is required before promotion.

## Evidence required before research dataset promotion

A reproducible dataset hash must be derivable from immutable evidence references and the declared temporal boundary. A second run with identical inputs must reproduce the same canonical hash.

## Evidence required before backtest promotion

Record dataset hash, config/parameter hash, temporal split, execution assumptions, transaction costs, missing-data policy, lookahead audit, baseline results, P&L/EV evidence, and multiple-testing correction.

## Relationship to Brain

Brain is the governance control plane and transition authority. Quant Engine's local state is a read-only projection and cannot override Brain state. Repository CI evidence proves repository execution only; it does not prove external runtime truth.

## Next strategic action

`QUANT-N010` may continue as a bounded local prerequisite. The first research implementation should be a **dataset-admission verifier / manifest builder**, not a trading model. It must prove temporal alignment and reproducibility before any alpha, backtest, Kelly, or P&L logic is enabled.
