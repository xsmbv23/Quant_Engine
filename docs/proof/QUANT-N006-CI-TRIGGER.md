# QUANT-N006 CI verification trigger

This file exists only to trigger the repository's pull-request test workflow for the exact N006 acquisition-quorum code currently on `main`.

It contains no source data, credentials, or research result.

The PR is a verification instrument, not a promotion event.
