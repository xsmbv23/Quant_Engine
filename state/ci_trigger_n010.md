# QUANT-N010 CI trigger

This file exists solely to trigger the repository's N010 admission workflow.

It carries no runtime truth, no database credentials, no source data, and no promotion authority.

Expected workflow evidence boundary:

- repository verifier execution only;
- external_runtime_truth = NOT_PROVEN;
- promotion = DENY;
- Brain remains authoritative;
- Room 02 remains LOCKED;
- Staircase remains LOCKED.
